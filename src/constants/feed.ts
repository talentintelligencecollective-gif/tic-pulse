/** Articles newer than this window stay in the default feed (client filter). */
export const FEED_FRESHNESS_DAYS = 14;

export const DEFAULT_FEED_LIMIT = 120;

/** Max rows loaded in the Watch tab grid (server-side cap). */
export const WATCH_TAB_VIDEO_LIMIT = 100;
